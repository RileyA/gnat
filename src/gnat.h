#ifndef gnat_gnat_H
#define gnat_gnat_H

#include <map>
#include <vector>
#include <string>
#include <list>

namespace gnat {

// Some common typedefs
typedef std::map Map;
typedef std::vector Vector;
typedef std::string String;
typedef std::list List;

}

#endif

